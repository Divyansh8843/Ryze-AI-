{
  "name": "ryze-ai-ui-generator",
  "version": "1.0.0",
  "description": "AI Agent for Deterministic UI Generation (Node + Python)",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm run dev",
    "start:ai": "cd ai-service && python app.py",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\" \"npm run start:ai\"",
    "build:frontend": "cd frontend && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
